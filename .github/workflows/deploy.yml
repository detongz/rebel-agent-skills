name: Deploy MySkills to Production

on:
  push:
    branches:
      - feat/moltiverse-openclaw
  workflow_dispatch:  # 允许手动触发部署

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install build tools for native modules
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 make g++

      - name: Cache root node modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      - name: Cache frontend node modules
        uses: actions/cache@v4
        with:
          path: frontend/node_modules
          key: ${{ runner.os }}-frontend-node-modules-${{ hashFiles('frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-frontend-node-modules-

      - name: Install dependencies
        run: |
          npm ci
          cd packages/shared && npm install

      - name: Install frontend dependencies
        run: |
          cd frontend && npm install --force

      - name: Build packages
        run: |
          echo "🔨 Building shared package..."
          cd packages/shared && npm run build

          echo "🔨 Building frontend..."
          cd ../../frontend && npm run build

      - name: Deploy to server
        env:
          DEPLOY_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        run: |
          echo "🚀 Starting deployment..."

          # 创建临时目录
          mkdir -p deploy-temp

          # 复制构建文件
          cp -r packages/shared/dist deploy-temp/shared
          cp -r frontend/.next deploy-temp/frontend
          cp -r packages/cli/dist deploy-temp/cli

          # 上传到服务器
          echo "Uploading files to server..."

          if [ -n "$DEPLOY_HOST" ]; then
            # SSH 方式部署
            echo "Deploying via SSH to $DEPLOY_HOST"

            # 创建临时 SSH 密钥文件
            echo "$DEPLOY_KEY" > deploy_key.pem
            chmod 600 deploy_key.pem

            # 停止旧服务
            ssh -i deploy_key.pem -o StrictHostKeyChecking=no \
              "$DEPLOY_HOST" "pm2 stop myskills2026 2>/dev/null || true"

            # 使用 scp 上传文件
            scp -i deploy_key.pem -o StrictHostKeyChecking=no -r \
              deploy-temp/* "$DEPLOY_HOST:$DEPLOY_PATH/"

            # 重启服务
            ssh -i deploy_key.pem -o StrictHostKeyChecking=no \
              "$DEPLOY_HOST" "cd $DEPLOY_PATH && pm2 restart myskills2026 2>/dev/null || pm2 start npm --name myskills2026"

            # 清理临时文件
            rm -f deploy_key.pem
            rm -rf deploy-temp

            echo "✅ Deployment completed!"
          else
            echo "⚠️  No DEPLOY_HOST configured, skipping deployment"
          fi

      - name: Cleanup
        if: always()
        run: rm -rf deploy-temp

      - name: Health check
        if: always()
        env:
          DEPLOY_URL: ${{ secrets.DEPLOY_URL }}
        run: |
          if [ -n "$DEPLOY_URL" ]; then
            echo "🏥 Running health check..."
            sleep 10

            # 检查 API 端点
            curl -f "$DEPLOY_URL/api/health" || echo "API health check failed"

            # 检查前端
            curl -f "$DEPLOY_URL" || echo "Frontend health check failed"

            echo "✅ Deployment health check completed"
          else
            echo "⚠️  No DEPLOY_URL configured, skipping health check"
          fi

      - name: Deployment summary
        if: always()
        run: |
          echo "================================"
          echo "🎉 Deployment Summary"
          echo "================================"
          echo ""
          echo "📦 Branch: ${{ github.ref_name }}"
          echo "🔗 Commit: ${{ github.sha }}"
          echo "⏰ Time: $(date -u '+%Y-%m-%d %H:%M:%S')"
          echo ""
          echo "Deployed components:"
          echo "  ✓ @myskills/shared"
          echo "  ✓ frontend"
          echo "  ✓ CLI"
          echo ""
          echo "🌐 URLs to check:"
          echo "  - Main site: ${{ secrets.DEPLOY_URL || 'Not configured' }}"
          echo "  - Health check: ${{ secrets.DEPLOY_URL }}/api/health"
