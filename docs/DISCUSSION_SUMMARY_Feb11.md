# 讨论纲要 - 2月11日晚

## 一、当前实现状态

### 已完成的核心组件

| 组件 | 状态 | 说明 |
|------|------|------|
| **MCP Server** | ✅ 完成 (2189行) | **14个工具** + 1个28日功能（⚠️`find_skills_for_budget` Smart Matching Engine - 当前mock）：list_skills, get_skill, tip_creator, register_skill, get_leaderboard, get_mon_balance, get_askl_balance, post_bounty, list_bounties, submit_audit, submit_task, assign_agents, complete_milestone, list_tasks |
| **CLI** | ✅ 完成 (133行) | **12个命令**：search, scan, install, tip, list, submit, leaderboard, publish, my-skills, login, add, whoami |
| **OpenClaw Plugin** | ✅ 完成 (228行) | **4个命令**（与CLI对齐）：`list`, `search`, `tip`, `balance` |
| **Smart Contract** | ✅ 部署 | ASKLToken on Monad testnet: `0xc1fFCAD15e2f181E49bFf2cBea79094eC9B5033A` |
| **Web Frontend** | ✅ 运行中 | https://myskills2026.ddttupupo.buzz |
| **Search API** | ✅ 完成 | 聚合 Vercel Skills + ClawHub 搜索结果 |
| **Database Schema** | ✅ 完成 | SQLite with 5 tables (skills, tips, faucet_claim, skill_likes, reviews) |

### 未完成的关键组件

| 组件 | 状态 | 说明 |
|------|------|------|
| **Security Scanner** | ✅ 基础完成 | `/api/scan` 实现 grep + npm audit 扫描 |
| **Real Skill Data** | ✅ 部分完成 | 依赖图从 package.json 提取，发布技能时存储 |
| **Dependency Graph** | ✅ 完成 | `/skills-map` 页面可视化真实依赖关系 |
| **Agent-to-Agent Payment** | ❌ Roadmap | 智能合约支持，但无Agent工作流实现 |

---

## 二、核心问题：Mock数据 vs 真实数据

### 问题现状

1. **本地数据库 (`data/skills.json`)**: 空，只有Mock数据
2. **搜索API (`/api/search`)**: 调用真实npx命令，但结果未被持久化
3. **数据库Schema**: 包含`repository`, `npm_package`字段，但无GitHub集成

### 用户提议方案

**Skill Metadata自动提交到GitHub，然后扫描获取**

#### 优势
- ✅ 符合Web3去中心化理念
- ✅ 创作者自主控制元数据
- ✅ 可通过GitHub API批量获取
- ✅ 与开源生态自然融合

#### 需要设计
1. **Skill Metadata格式** - 标准化YAML/JSON
2. **GitHub扫描机制** - 如何发现、解析、验证
3. **15日前可行性** - 时间评估

---

## 三、Skill Metadata格式设计

### 方案A: skill.md 标准格式

```markdown
# MySkills Protocol Metadata

name: "Solidity Auditor"
description: "AI-powered Solidity code security auditor"
version: "1.0.0"
platform: "claude-code"
category: "security"

# 收款信息（核心）
payment:
  address: "0x1234...5678"  # 创作者钱包
  split: 98                   # 收益比例%

# 依赖信息
dependencies:
  - "@anthropic-ai/sdk"
  - "solidity-parser"

# 安全报告（可选）
security:
  report_ipfs: "Qm..."      # 深度扫描报告CID
  score: 92                  # 综合评分
  scanned_at: "2026-02-10"

# 统计信息
stats:
  downloads: 1520
  tips: "1500000000000000000000"  # 1500 ASKL

# 社交链接
links:
  repository: "https://github.com/user/solidity-auditor"
  npm: "@myskills/solidity-auditor"
  homepage: "https://myskills.dev/solidity-auditor"
```

### 方案B: package.json 扩展字段

```json
{
  "name": "@myskills/solidity-auditor",
  "version": "1.0.0",
  "description": "AI-powered Solidity code security auditor",
  "myskills": {
    "platform": "claude-code",
    "category": "security",
    "payment": {
      "address": "0x1234...5678",
      "split": 98
    },
    "repository": "https://github.com/user/solidity-auditor"
  }
}
```

### 推荐：**方案A - skill.md**

**理由:**
- ✅ 与现有OpenClaw生态一致 (`skill.md` already exists)
- ✅ 人类可读，易于编辑
- ✅ 支持多平台（不仅限于npm）
- ✅ 验证后可存储到IPFS

---

## 四、GitHub扫描机制设计

### 扫描流程

```
┌─────────────────────────────────────────────────────────────────┐
│                    GitHub Discovery Engine                    │
├─────────────────────────────────────────────────────────────────┤
│                                                            │
│ 1. 搜索发现                                               │
│    ├── GitHub API: topic:agent-skill + language:typescript  │
│    ├── GitHub API: topic:myskills-protocol                │
│    └── 手动提交队列                                       │
│                                                            │
│ 2. 解析元数据                                             │
│    ├── 查找 skill.md 或 package.json                       │
│    ├── 验证必需字段                                       │
│    └── 提取收款地址                                       │
│                                                            │
│ 3. 安全扫描（异步）                                        │
│    ├── 基础扫描（免费）                                   │
│    │   ├── 代码模式检测                                    │
│    │   ├── npm audit                                       │
│    │   └── 依赖分析                                       │
│    └── 深度扫描（付费）                                   │
│        └── VirusTotal API + 沙箱执行                        │
│                                                            │
│ 4. 链上注册                                               │
│    ├── 生成 skillId = keccak256(name:repo:creator)        │
│    ├── 调用 registerSkill(skillId, name, creator)          │
│    └── 存储到本地数据库                                   │
│                                                            │
│ 5. 索引更新                                               │
│    └── 可被搜索API查询                                      │
│                                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 实现组件

#### 1. GitHub Discovery Service (`/packages/github-scanner`)

```typescript
class GitHubSkillScanner {
  // 按topic搜索
  async searchByTopic(topic: string): Promise<Repository[]>

  // 解析skill.md
  async parseSkillMetadata(repoUrl: string): Promise<SkillMetadata>

  // 验证metadata
  validateMetadata(meta: SkillMetadata): ValidationResult
}
```

#### 2. API Endpoint (`/api/discover/github`)

```typescript
// GET /api/discover/github?topic=monad&sort=stars
// 返回：GitHub搜索结果 + 已注册状态
```

#### 3. 批量导入脚本 (`/scripts/import-from-github.ts`)

```bash
# 导入所有带myskills标签的repo
npx myskills import:github --topic myskills-protocol --limit 100

# 导入特定repo
npx myskills import:github --repo user/repo
```

---

## 五、15日前可行性分析

### 时间线

```
今日 (2月11日)
├── 讨论/确认方案 ✓
└── 基础设施准备

2月12日 (周三)
├── 实现skill.md解析器
├── GitHub API集成
└── 基础安全扫描

2月13日 (周四)
├── 批量导入工具
├── 链上注册集成
└── 测试真实数据

2月14日 (周五)
├── Demo视频录制
├── 前端展示调整
└── 提交材料准备

2月15日 (周六) 🔴
└── MOLTIVERSE提交截止
```

### 最小可行方案 (MVP for Feb 15)

**能做的:**
- ✅ skill.md格式定义
- ✅ GitHub API搜索（发现repos）
- ✅ 解析skill.md/提取metadata
- ✅ 基础安全扫描（grep + npm audit）- 已完成
- ✅ 依赖关系图可视化 - 已完成
- ✅ 手动导入5-10个真实技能

**不能做的:**
- ❌ 完整的安全扫描器（VirusTotal + 沙箱）
- ❌ 自动批量扫描所有GitHub
- ❌ Agent-to-Agent支付工作流
- ❌ 深度依赖分析（版本追踪、循环检测等）

### 15日提交策略调整

**原始定位**: Agent Marketplace + Smart Matching
**调整为**: **Agent Skill Directory + Security Preview**

**展示内容:**
1. **真实技能数据** - 从GitHub导入5-10个真实技能
2. **安全扫描原型** - 展示基础扫描功能（代码模式 + npm audit）
3. **链上打赏** - 展示真实的支付流程
4. **MCP Server集成** - 实际可用的工具集
5. **依赖关系图** - 可视化技能间的依赖关系（已完成）

**预留28日功能:**
- Agent-to-Agent自动支付工作流
- 完整的安全评分系统（0-100分）
- 深度扫描服务（付费）
- Agent评估与排行榜
- **Smart Matching Engine** (`find_skills_for_budget` - 当前为 mock 数据)

---

## 六、数据源冷启动策略

### 方案: 聚合开源生态 + 安全扫描增值

| 数据源 | 技能数 | 获取方式 | 我们的增值 |
|--------|--------|----------|------------|
| **Vercel Skills** | 500+ | `npx skills find` | +安全扫描 |
| **ClawHub** | 1700+ | `npx clawhub search` | +安全扫描 |
| **GitHub (agent-skill)** | 50+ | GitHub API | +注册到链 + 安全扫描 |

### 产品定位: **不是竞争者，是安全层 + 聚合器**

```
现有生态                           MySkills Protocol
┌──────────────┐                ┌──────────────────────┐
│ Vercel Skills │ ──────┐       │  🔍 统一搜索        │
└──────────────┘        │       │  🛡️ 安全扫描         │
                       ├──────▶│  💰 链上打赏         │
┌──────────────┐        │       │  📊 统一评分         │
│   ClawHub    │ ──────┘       │  🔗 跨平台索引        │
└──────────────┘                └──────────────────────┘
        │                              │
        │                              ▼
        │                    ┌──────────────────┐
        │                    │ 技能仍在原平台    │
        │                    │ 我们只是加层     │
        │                    └──────────────────┘
```

---

## 七、今晚讨论要点

### 1. 安全检测实现方案

**问题**: 15日前能做到什么程度？

**选项A**: 基础扫描原型
- 代码模式检测（grep关键字）
- npm audit漏洞检查
- 计算简单安全评分
- **时间**: 2-3天

**选项B**: 完整扫描器（推到28日）
- 包含VirusTotal API集成
- 沙箱执行分析
- 完整0-100评分系统
- **时间**: 7-10天

**建议**: 选择A，15日展示原型，28日完成完整版

### 2. 宣发Twitter策略

**已有准备**:
- ✅ 5天推文内容日历 (SOCIAL_MEDIA_GROWTH_PLAN.md)
- ✅ 10+ 推文模板库 (TWEET_TEMPLATES.md)
- ✅ KOL名单和互动策略

**讨论要点**:
- 是否需要录制Demo视频后才开始推流？
- 优先Web3开发者还是AI开发者？
- 是否参与Monad官方Discord讨论？

### 3. 15日提交重点

**核心展示**:
1. **真实数据** - 从GitHub导入的真实技能
2. **可用性** - MCP Server实际可安装使用
3. **支付演示** - 展示真实的tip交易
4. **安全扫描** - 基础版本的原型

**不展示**:
- Agent自动雇佣工作流（28日功能）
- 深度安全评分系统
- 完整的智能合约审计报告

---

## 八、下一步行动 (如果方案确认)

### 明日 (2月12日) 开发任务

1. **定义skill.md标准格式** - 文档 + 示例
2. **实现GitHub Scanner** - `/packages/github-scanner`
3. **基础安全扫描** - ✅ 已完成（grep + npm audit）
4. **导入真实技能** - 5-10个GitHub repos
5. **实现 skill.md 解析器** - 读取和验证元数据

### 测试验证

```bash
# 测试GitHub扫描
npx myskills scan:github --topic agent-skill

# 导入到数据库
npx myskills import --from-github --repo user/repo

# 验证前端显示
curl https://myskills2026.ddttupupo.buzz/api/skills
```

---

## 附录：技术决策记录

### A. 为什么用skill.md而不是package.json？

- OpenClaw生态已使用skill.md
- 支持非npm技能（Coze, Manus）
- 人类可读，便于验证

### B. 为什么15日不做完整安全扫描？

- VirusTotal API需要测试和调优
- 沙箱执行需要额外基础设施
- 评分算法需要大量测试数据
- 优先实现核心价值（支付），安全扫描作为28日亮点

### C. 数据库Schema已预留字段

```sql
-- schema.sqlite.sql 已包含:
repository TEXT      -- GitHub仓库URL
npm_package TEXT    -- npm包名
github_stars INTEGER -- Stars缓存
```

只需实现GitHub Scanner即可填充这些字段。
